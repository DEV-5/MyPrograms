
 int system(const char*(clear));
 int scanf(const char*,...);
 int printf(const char*,...);

  int main()
 {
     int a,b,c;
     char ch=0;
  system("clear");
  if(printf("the data entered is %d\n",ch||15))
  {
   printf("the size of long double is %d\n",sizeof(long double));
   printf("the size of double is %d\n",sizeof(double));
   printf("the size of l is %d\n",sizeof("this may be known"));
  }
  return 0;
 }
